import { Icon } from 'antd';
import { iconfontUrl as scriptUrl } from '@/constants';

// 使用：
// import { IconFont } from '@components';
// <IconFont type='icon-demo' className='xxx-xxx' />
export default Icon.createFromIconfontCN({ scriptUrl });
